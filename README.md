# claude-overlay

Dynamic-Island-style overlay for Claude Code hooks on Windows 11.

**Status:** code complete (Tasks 1–16). v1 validation (`cargo tauri build`, install scripts, 22-item manual checklist) deferred to a Windows machine with Rust + Tauri toolchain.

See `docs/install.md` to install.
See `docs/2026-04-24-claude-overlay-design.md` for the full design.
